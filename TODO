TODO
====

 * automatic obsoleting
 * udeb split -> into debian-installer hierarchy
 * move crontab and config to an example section
 * optimize archive-reindex speed (find and parse .changes)
 * check if size matches for files uploaded
 * locking of actions
 * the code to move the files from incoming to unchecked should be split from
   verify_md5sum
 * support tag filters || arch pool split?
 * rewrite in perl?

